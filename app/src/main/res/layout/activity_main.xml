<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#EDEDED">

    <!-- LEFT SIDEBAR -->
    <LinearLayout
        android:id="@+id/sidebarContainer"
        android:layout_width="72dp"
        android:layout_height="0dp"
        android:background="@color/Forest_Green"
        android:orientation="vertical"
        android:padding="8dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <!-- HOME BUTTON (STATIC) -->
        <ImageButton
            android:id="@+id/btnHome"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:layout_marginBottom="12dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Home"
            android:foreground="@drawable/ic_world"
            android:scaleType="centerInside" />

        <!-- DYNAMIC BUTTON CONTAINER -->
        <LinearLayout
            android:id="@+id/dynamicButtonContainer"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical" />

    </LinearLayout>


    <!-- TOP BAR -->
    <FrameLayout
        android:id="@+id/topBarContainer"
        android:layout_width="0dp"
        android:layout_height="64dp"
        android:background="@color/Celadon"
        app:layout_constraintStart_toEndOf="@id/sidebarContainer"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <LinearLayout
            android:id="@+id/worldContainer"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:orientation="horizontal"
            android:gravity="center"
            android:visibility="gone">

            <Spinner
                android:id="@+id/spinnerWorldSelector"
                android:layout_width="160dp"
                android:layout_height="wrap_content"/>

            <Button
                android:id="@+id/btnCreateWorld"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Create World"
                android:layout_marginStart="12dp"/>
        </LinearLayout>

        <Button
            android:id="@+id/btnCreateWorldOnly"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:text="Create World"
            android:visibility="gone"/>

    </FrameLayout>



    <!-- MAIN CONTENT (FRAGMENT HOST) -->
    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/mainFragmentContainer"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@id/topBarContainer"
        app:layout_constraintStart_toEndOf="@id/sidebarContainer"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"/>

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/btnCreateNote"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="24dp"
        app:srcCompat="@drawable/ic_magic"
        app:backgroundTint="@color/Forest_Green"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />


</androidx.constraintlayout.widget.ConstraintLayout>
